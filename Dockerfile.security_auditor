FROM python:3.12-slim

WORKDIR /opt/defenseops-lab

COPY security_auditor_tool.py .
COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt || echo "No requirements"

VOLUME ["/opt/defenseops-lab/reports"]

ENTRYPOINT ["python3", "security_auditor_tool.py"]

# Dockerfile.network
FROM python:3.9-alpine

# Install nmap for native scanning
RUN apk add --no-cache nmap

# Set working directory
WORKDIR /app

# Copy the correct network scanner tool
COPY network_vuln_scanner_tool.py .

# Set entrypoint
ENTRYPOINT ["python3", "network_vuln_scanner_tool.py"]

